[build]
  command = "npm run build"
  publish = "dist"

[functions]
  # Directorio donde están las funciones Lambda
  directory = "dist/functions"

[[redirects]]
  # Redirige todas las rutas a la función principal
  from = "/*"
  to = "/.netlify/functions/main"
  status = 200
